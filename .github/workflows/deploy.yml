name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout MiniOS GUI test dependency
        uses: actions/checkout@v4
        with:
          repository: minios-linux/minios-gui
          path: .ci/minios-gui

      - name: Install test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pytest python3-yaml python3-gi gir1.2-gtk-3.0 python3-websockets

      - name: Run Python test suite
        run: python3 -m pytest -q
        env:
          PYTHONPATH: ${{ github.workspace }}/.ci/minios-gui

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Validate recipes
        run: python3 tools/build_recipes.py --validate

      - name: Build recipes.json
        run: python3 tools/build_recipes.py -o web/public/data/recipes.json

      - name: Check recipe artifacts are up to date
        run: |
          if [ -n "$(git status --porcelain -- web/public/data/recipes.json web/public/data/recipes-index*.json web/public/data/recipes/)" ]; then
            git status --short -- web/public/data/recipes.json web/public/data/recipes-index*.json web/public/data/recipes/
            echo "::error::Recipe artifacts are out of date. Run 'python3 tools/build_recipes.py -o web/public/data/recipes.json' and commit the generated files."
            exit 1
          fi

      - name: Generate languages.json
        run: |
          python3 -c "
          import json, glob, os
          langs = []
          for f in sorted(glob.glob('web/public/translations/*.json')):
              if os.path.basename(f) == 'languages.json':
                  continue
              code = os.path.basename(f).replace('.json', '')
              with open(f) as fh:
                  meta = json.load(fh).get('_meta', {})
              langs.append({'code': code, 'name': meta.get('name', code), 'flag': meta.get('flag', '')})
          with open('web/public/translations/languages.json', 'w') as fh:
              json.dump(langs, fh)
          print(f'Generated languages.json with {len(langs)} languages')
          "

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: web

      - name: Run web unit tests
        run: npm run test
        working-directory: web

      - name: Lint
        run: npm run lint
        working-directory: web

      - name: Build
        run: npm run build
        working-directory: web

      - name: Create .nojekyll
        run: touch ./web/dist/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./web/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

id: fortunate-app
name: Fortunate
description: GNUstep fortune cookies
longDescription: |
  <p>This displays a quotation in a window. Fortunate is a Cocoa/Objective-C graphical front-end to the command-line BSD fortune which, since the dawn of time, has been providing countless seconds of fun each time a user logs in.</p>
  
categoryId: games
icon: Gamepad2
method: apt
level: "auto"
compression: zstd
packages:
  - fortunate.app
distributions:
  include:
    - name: trixie
      architectures: [amd64, i386]
tags:
  - fortune
  - quotation
  - cookie
iconSources:
  - cached: fortunate.app_Fortunate.png
    width: 64
    height: 64
enabled: true
order: 99

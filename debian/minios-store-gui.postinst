#!/bin/sh
set -e

case "$1" in
    configure)
        # Update desktop database to register MIME type handler
        if command -v update-desktop-database >/dev/null 2>&1; then
            update-desktop-database /usr/share/applications || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
